# AI Assistant Guide

This repository builds a Pi workflow scaffold compatible with Claude/Codex habits.

Use `mise` tasks as the command interface. Do not add ad hoc shell scripts when a
`mise.toml` task is enough.

Keep this public repository limited to source code and user-facing documentation.
Do not commit internal planning, execution logs, private infrastructure details,
credentials, or machine-specific absolute paths.
